GeneratedAt: "2026-03-16T18:26:49Z"
Plan: ios-fitness-tracker-app-plan
Sprints:
- Features:
  - FeatureID: ios-fitness-tracker-app-feat-session-tracking
    FeatureTitle: Gym Session Logging with Rest Timer & PR Detection
    Tasks:
    - AcceptanceCriteria:
      - PRDetector correctly identifies a new 1RM-equivalent PR when weight × reps
        exceeds historical best
      - PRDetector returns no PR when the set does not exceed the historical best
      - PRDetector handles empty history (first-ever set for an exercise) as a PR
      AgentType: backend-engineer
      Dependencies: []
      Description: Build PRDetector as a framework-free domain service that compares
        a newly logged set (weight × reps) against historical best for the same exercise.
        It should return a PRResult indicating whether a new PR was achieved and the
        previous best for display. Unit test with plain XCTest.
      EstimatedEffort: S
      Files:
      - FitnessTracker/Domain/PRDetector.swift
      ID: task-ios-fitness-tracker-app-feat-session-tracking-1
      Title: Implement PRDetector domain logic
    - AcceptanceCriteria:
      - State transitions (start, pause, resume, finish) update sessionState correctly
        and are reflected in published state
      - Rest timer counts down from the configured duration and resets when a new
        set is logged; haptic fires on expiry
      - Saving a set invokes PRDetector and exposes isPR flag in the resulting set
        model
      - Calling finish() on an active session transitions to complete state with a
        populated summary
      AgentType: backend-engineer
      Dependencies:
      - task-ios-fitness-tracker-app-feat-session-tracking-1
      Description: Build SessionViewModel as an @Observable state machine transitioning
        through idle → active → paused → complete. Manage the active WorkoutSession
        (add sets, mark completion, track elapsed time). Integrate Timer.publish for
        a configurable rest timer (default 90 s) that triggers UIImpactFeedbackGenerator
        haptics on expiry. On set save, call PRDetector and surface any PR result
        to the view.
      EstimatedEffort: M
      Files:
      - FitnessTracker/Features/Workout/SessionViewModel.swift
      ID: task-ios-fitness-tracker-app-feat-session-tracking-2
      Title: Implement SessionViewModel state machine with rest timer
    - AcceptanceCriteria:
      - ExerciseSetRow displays previous session values inline and shows a PR badge
        when the set is flagged
      - RestTimerView renders a countdown ring that animates down and resets on new
        set; shows haptic-trigger state visually
      - SessionSummaryView lists total volume, session duration, and any PRs achieved
        during the session
      AgentType: backend-engineer
      Dependencies:
      - task-ios-fitness-tracker-app-feat-session-tracking-2
      Description: Implement ExerciseSetRow (inline weight/reps input with previous-session
        data displayed alongside and a PR badge), RestTimerView (circular countdown
        with pause/reset controls), and SessionSummaryView (post-session recap showing
        volume, duration, and PR highlights). All components bind to SessionViewModel
        via @Bindable or closure callbacks.
      EstimatedEffort: M
      Files:
      - FitnessTracker/Features/Workout/Components/ExerciseSetRow.swift
      - FitnessTracker/Features/Workout/Components/RestTimerView.swift
      - FitnessTracker/Features/Workout/Components/SessionSummaryView.swift
      ID: task-ios-fitness-tracker-app-feat-session-tracking-3
      Title: Build session UI components
    - AcceptanceCriteria:
      - SessionView renders all exercises with set tables and previous-session data
        populated from SwiftData
      - Finishing a session saves a WorkoutSession record (volume, duration, PR count)
        to SwiftData and is queryable
      - An HKWorkout with correct start/end dates and energy metadata is written via
        HealthKitService on session complete
      - Navigating away from an active session without finishing prompts the user
        before discarding
      AgentType: backend-engineer
      Dependencies:
      - task-ios-fitness-tracker-app-feat-session-tracking-3
      Description: Build SessionView to host the exercise list with ExerciseSetRows,
        embed RestTimerView, and navigate to SessionSummaryView on finish. On session
        completion, persist volume/duration/PR summary to SwiftData via the workout
        repository and write an HKWorkout through HealthKitService. Ensure the session
        is correctly torn down and the view transitions back to idle.
      EstimatedEffort: M
      Files:
      - FitnessTracker/Features/Workout/SessionView.swift
      ID: task-ios-fitness-tracker-app-feat-session-tracking-4
      Title: Compose SessionView and wire SwiftData + HealthKit persistence
  - FeatureID: ios-fitness-tracker-app-feat-progress-analytics
    FeatureTitle: Progress Charts for Weight, Measurements & Strength
    Tasks:
    - AcceptanceCriteria:
      - Aggregation queries run on a background @ModelActor context and never block
        the main thread
      - Down-sampled weekly averages are returned for All-time range; raw data for
        1W/1M/3M
      - 1RM estimates use the Epley formula and are grouped per exercise
      - Published state updates drive SwiftUI view refreshes via @Observable or @Published
      AgentType: backend-engineer
      Dependencies: []
      Description: Create ProgressViewModel as a @ModelActor-backed class that queries
        SwiftData for bodyweight entries, body measurements, and per-exercise sets
        to compute volume and Epley 1RM estimates. Support 1W, 1M, 3M, and All time
        ranges, down-sampling All-time data to weekly averages so aggregation completes
        under 300 ms.
      EstimatedEffort: L
      Files:
      - FitnessTracker/Features/Progress/ProgressViewModel.swift
      ID: task-ios-fitness-tracker-app-feat-progress-analytics-1
      Title: Implement ProgressViewModel with background aggregation and time-range
        filtering
    - AcceptanceCriteria:
      - All three chart views render correctly for each time range (1W, 1M, 3M, All)
      - Changing the time range in TimeRangePicker triggers a new ProgressViewModel
        fetch and chart re-render
      - StrengthChartView allows selecting a single exercise and displays its 1RM
        trend
      - Charts show empty-state placeholders when no data is available
      AgentType: frontend-engineer
      Dependencies:
      - task-ios-fitness-tracker-app-feat-progress-analytics-1
      Description: Implement WeightChartView, StrengthChartView, and TimeRangePicker
        using Swift Charts. WeightChartView renders a bodyweight trend line; StrengthChartView
        renders per-exercise 1RM lines with a picker to switch exercises. TimeRangePicker
        is a segmented control binding to the view model's selected range.
      EstimatedEffort: M
      Files:
      - FitnessTracker/Features/Progress/Components/WeightChartView.swift
      - FitnessTracker/Features/Progress/Components/StrengthChartView.swift
      - FitnessTracker/Features/Progress/Components/TimeRangePicker.swift
      ID: task-ios-fitness-tracker-app-feat-progress-analytics-2
      Title: Build Swift Charts components and TimeRangePicker
    - AcceptanceCriteria:
      - MeasurementLogView validates required fields and saves a new measurement entry
        to SwiftData on submit
      - ProgressView displays all chart components stacked with the shared TimeRangePicker
        controlling all ranges
      - Newly logged measurements appear in WeightChartView without requiring app
        restart
      - ProgressView is reachable from the main tab bar
      AgentType: frontend-engineer
      Dependencies:
      - task-ios-fitness-tracker-app-feat-progress-analytics-2
      Description: Implement MeasurementLogView as a sheet form for logging body measurements
        (weight, chest, waist, hips, etc.) that persists entries via ProgressViewModel.
        Compose ProgressView to host TimeRangePicker, WeightChartView, StrengthChartView,
        and a measurements section with a button to present the log form.
      EstimatedEffort: M
      Files:
      - FitnessTracker/Features/Progress/Components/MeasurementLogView.swift
      - FitnessTracker/Features/Progress/ProgressView.swift
      ID: task-ios-fitness-tracker-app-feat-progress-analytics-3
      Title: Build MeasurementLogView form and assemble ProgressView
  Sprint: 5
