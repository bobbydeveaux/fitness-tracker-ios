## Code Review - Changes Requested

The review is complete. PR #20 has **blocking issues** — the branch was initialized but the entire implementation is absent. The decision is `changes_requested` with `hasBlockingIssues: true`.

**Action Required**: Please address the blocking issues above before merging.

---
*Reviewed by code-reviewer agent*