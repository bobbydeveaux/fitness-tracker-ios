## Code Review - Changes Requested

## ❌ BLOCKING ISSUES FOUND

### PR #44 — Implement NotificationScheduler with UNUserNotificationCenter

---

### Summary

This PR is a **WIP draft** with **zero implementation**. The only diff in the branch is the deletion of `.claude-output.json` (a build artifact from a prior PR). The required file `FitnessTracker/Services/NotificationScheduler.swift` **does not exist** anywhere in the codebase.

---

### Blocking Issues

**[BLOCKING] Implementation is entirely missing**

The PR's stated goal is to implement `NotificationScheduler` per issue #27, but none of the required code has been written. All four acceptance criteria are unmet:

| Acceptance Criterion | Status |
|---|---|
| `requestPermission()` returns auth status and updates a published property | ❌ Not implemented |
| `scheduleReminders(days:time:)` creates one `UNNotificationRequest` per weekday with correct calendar trigger | ❌ Not implemented |
| Calling `scheduleReminders` again removes previously scheduled fitness-reminder notifications before re-adding | ❌ Not implemented |
| `cancelAll()` removes all pending fitness-reminder notifications | ❌ Not implemented |

The file specified in tasks.yaml (`FitnessTracker/Services/NotificationScheduler.swift`) does not exist. The only change in this branch is removal of `.claude-output.json`, which appears to be leftover from a previous sprint's work.

---

### What Needs to Be Done

1. Create `FitnessTracker/Services/NotificationScheduler.swift` with:
   - A protocol (e.g. `NotificationSchedulerProtocol`) for testability, following the pattern used by `HealthKitService`
   - `requestPermission()` async method that calls `UNUserNotificationCenter.current().requestAuthorization(options:)` and publishes the resulting `UNAuthorizationStatus`
   - `scheduleReminders(days:time:)` that removes existing `fitness-reminder-*` pending requests, then adds one `UNNotificationRequest` per selected weekday using `UNCalendarNotificationTrigger`
   - `cancelAll()` that removes all pending `fitness-reminder-*` notifications

2. Unit tests covering the scheduler logic (using a mock `UNUserNotificationCenter` or similar test double)

3. Update `.claude-output.json` with the correct metadata for this task

---

This PR cannot be merged in its current state as it contains no feature implementation.

**Action Required**: Please address the blocking issues above before merging.

---
*Reviewed by code-reviewer agent*
