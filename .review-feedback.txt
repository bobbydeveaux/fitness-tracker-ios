## Code Review - Changes Requested

The review is complete. This PR has a **blocking issue**: the entire feature implementation is missing. The PR only deletes a leftover metadata file from a prior task, while `OnboardingViewModel.swift` and all related code were never created.

---

## ❌ BLOCKING ISSUES FOUND

**PR #16 — WIP: Implement HealthKitService singleton with permissions, read, and write**

---

### Summary

This PR is in **DRAFT** state and contains **zero new code**. The only change is the deletion of `.claude-output.json` (a metadata file from a previous task). The `HealthKitService.swift` file remains an empty stub — no HealthKit implementation has been delivered.

---

### BLOCKING: Core Functionality Not Implemented

**File:** `FitnessTracker/Services/HealthKitService.swift`

The service is entirely a stub. Every acceptance criterion from issue #10 is unmet:

| Acceptance Criterion | Status |
|---|---|
| `requestAuthorisationIfNeeded()` requests the 3 HKQuantityTypes | ❌ Empty body |
| `readDailyStats()` returns real data via `HKStatisticsCollectionQuery` | ❌ Returns zeroed `DailyStats()` |
| `saveWorkout(duration:)` writes an `HKWorkout` with `.traditionalStrengthTraining` | ❌ Empty body |
| `DashboardViewModel` integration with `readDailyStats()` | ❌ `DashboardViewModel` does not exist |

Specific missing implementation details:
- No `import HealthKit` — the framework is never imported
- No `HKHealthStore` instance created or referenced
- `requestAuthorisationIfNeeded()` has an empty body; no permission request occurs
- `readDailyStats()` always returns `DailyStats()` with all zeroes — no queries executed
- `saveWorkout(duration:)` has an empty body; nothing is written to HealthKit
- The `init()` is `internal` (not `private`), breaking the singleton contract — external code can bypass `.shared` and create additional instances
- No `DashboardViewModel` exists to satisfy the fourth acceptance criterion

---

### Additional Issues (Non-Blocking)

**No tests for HealthKitService**
There is no `HealthKitServiceTests.swift`. Given HealthKit requires a real device/entitlement for most operations, at minimum a mock/protocol abstraction should be present so the service can be tested with a test double.

**`docsUpdated` is empty in the deleted `.claude-output.json`**
While that file was from a prior task, this PR also doesn't update any documentation. If the LLD/HLD describe the HealthKit integration design, they should be confirmed accurate once implementation lands.

---

### What Needs to Happen Before Merge

1. Implement `HealthKitService.swift` with `import HealthKit`, an `HKHealthStore` property, and real logic in all three methods
2. Make `init()` private so the singleton cannot be bypassed
3. Create `DashboardViewModel` (or extend it if it exists elsewhere) to call `readDailyStats()` on scene foreground
4. Add `HealthKitServiceTests.swift` with protocol-based mocking so the service logic is testable without a live device
5. Move the PR out of DRAFT once the above is complete

**Action Required**: Please address the blocking issues above before merging.

---
*Reviewed by code-reviewer agent*
